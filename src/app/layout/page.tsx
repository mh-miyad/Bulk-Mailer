import MainEmailBuilder from "@/components/EmailComponet/MainEmailBuilder";

const EmailLayout = () => {
  return (
    <div>
      <MainEmailBuilder />
    </div>
  );
};

export default EmailLayout;
